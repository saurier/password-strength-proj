import { StrengthEventDirective } from './strength-event.directive';

describe('StrengthEventDirective', () => {
  it('should create an instance', () => {
    const directive = new StrengthEventDirective();
    expect(directive).toBeTruthy();
  });
});
